// Shared contracts: item record shape, event envelope, wire protocol constants. The one definition both the service and the layer import (Phase 0, plan Task 0.1). Not implemented yet.

module.exports = {};
